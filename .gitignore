# Python
__pycache__/
*.py[cod]
*$py.class
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# OS
.DS_Store

# Editor
.idea/
.vscode/
*.swp

# Dogfood SDLC state: never ship the skill's own .sdlcfa/ if it is run on this
# repo, so a clone does not present a populated root .sdlcfa/ that the resume
# rule would mistake for the user's live project.
/.sdlcfa/
